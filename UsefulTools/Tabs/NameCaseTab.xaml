<UserControl
    x:Class="UsefulTools.Tabs.NameCaseTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧：输入区域 -->
        <Grid Grid.Column="0" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="CaseErrorTextBlock" Grid.Row="0"
                   Foreground="Red" TextWrapping="Wrap" Visibility="Collapsed"/>

            <ComboBox x:Name="CaseFormatComboBox" Grid.Row="1"
                  Header="目标命名格式"
                  SelectedIndex="0">
                <ComboBoxItem Content="camelCase"/>
                <ComboBoxItem Content="PascalCase"/>
                <ComboBoxItem Content="snake_case"/>
            </ComboBox>

            <TextBox x:Name="CaseInputTextBox" Grid.Row="2"
                 Header="请输入 JSON 或 XML 字符串"
                 PlaceholderText="支持 JSON 对象/数组 或 XML 文档..."
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"/>

            <Button x:Name="CaseConvertButton" Grid.Row="3"
                Content="转换"
                HorizontalAlignment="Left"
                Padding="16,8"/>
        </Grid>

        <!-- 右侧：输出区域 -->
        <Grid Grid.Column="1" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="CaseOutputTextBox" Grid.Row="0"
                 Header="转换结果"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 PlaceholderText="转换后的结果将显示在这里..."/>

            <Button x:Name="CaseCopyButton" Grid.Row="1"
                Content="一键复制"
                HorizontalAlignment="Right"
                Padding="16,8"/>
        </Grid>
    </Grid>
</UserControl>
